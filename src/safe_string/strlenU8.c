/*---------------------------------------------------------------------------
|
|--------------------------------------------------------------------------*/

#include "libs_support.h"
#include "util.h"


/* ------------------------------- strlenU8 -----------------------------------

   'Safe' version. Does use size_t so can't roam to the end of an MCU.
*/

PUBLIC U8 strlenU8(C8 const* str)
{
   for(U8 i = 0; i < 0xFF; i++, str++) {
      if(*str == '\0') {
         return i; }}
   return 0xFF;
}

// ------------------------------- eof -----------------------------------
