/*---------------------------------------------------------------------------
|
|
|
|--------------------------------------------------------------------------*/

#include "libs_support.h"
#include "util.h"

/*-----------------------------------------------------------------------------------------
|
|  IsHexASCII()
|
|  Return '1' is 'ch' is a HexASCII character, uppercase or lowercase.
|
------------------------------------------------------------------------------------------*/

PUBLIC BIT IsHexASCII(U8 ch)
{
   return (ch <= '9' && ch >= '0' ) || ( ch >= 'A' && ch <= 'F' ) || (ch >= 'a' && ch <= 'f');
}


// --------------------- eof --------------------------------  -
