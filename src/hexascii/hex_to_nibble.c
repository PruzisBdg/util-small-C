/*---------------------------------------------------------------------------
|
|  
|   
|--------------------------------------------------------------------------*/

#include "libs_support.h"
#include "util.h"

/*-----------------------------------------------------------------------------------------
|
|  HexToNibble()
|
|  Return the numerical value of  ASCII Hex 'ch'. If 'ch' isn't legal then return 0xFF 
|
------------------------------------------------------------------------------------------*/

PUBLIC U8 HexToNibble(U8 ch)
{
   if (ch <= '9' && ch >= '0' )     // '0' - '9'?
   {
      return ch - '0';
   }
   else if( ch >= 'A' && ch <= 'F' )// 'A' - 'F'?
   {
      return ch - 'A' + 10;
   }
   else if( ch >= 'a' && ch <= 'f' )// 'a' - 'f'?
   {
      return ch - 'a' + 10;
   }
   else                             // else not hex
   {
      return 0xFF;
   }
}


// --------------------- eof --------------------------------  -    
